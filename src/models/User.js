export class Employee{

    constructor(Name, PictureId, Role, UserId, UserName){
        this.Name = Name
        this.PictureId = PictureId
        this.Role = Role
        this.UserId = UserId
        this.UserName = UserName
    }
}